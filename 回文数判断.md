```c
#include <stdio.h> 

int main()
{
	int a,b, c=0, n;
	scanf("%d",&n);
	a = n;
	while (n !=0)
	{
		b = n % 10;
		c = c * 10 + b;
		n /= 10;
	}

	if (a == c)
		printf("回文数");
	else
		printf("非回文数");
	
	return 0;
}
```
